name: Deploy

on:
  workflow_dispatch:

jobs:
  managed-deployments:
    runs-on: ubuntu-latest
    steps:
      - name: Explain deployment ownership
        run: |
          echo "Frontend deployments are managed by Vercel's Git integration."
          echo "Backend deployments and database migrations are managed by Render."
