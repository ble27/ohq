import { z } from 'zod';

// Email/password auth schemas removed — Google OAuth only.

/** Placeholder export so the module remains valid if re-imported later. */
export const AuthSchemaDeprecated = z.object({});
