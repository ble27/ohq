# Render (and other hosts) set NODE_ENV=production during the build step,
# which makes `npm install` skip devDependencies by default — but the build
# needs typescript, @types/*, and prisma (all devDependencies) to compile.
# This forces npm to always install devDependencies, regardless of NODE_ENV.
include=dev
